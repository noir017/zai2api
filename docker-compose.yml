services:
  zai2api:
    image: ghcr.io/noir017/zai2api:latest
    container_name: zai2api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
